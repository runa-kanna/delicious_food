<h1>管理者側の投稿詳細ページ</h1>

<div>

  <% @food.images.each do |image| %>
      <%= image_tag(image, width:100) %>
  <% end %>

  <p>ショップ名：<%= @food.title %></p>
  <p>説明：<%= @food.body %></p>
  <p>投稿ユーザー画像：<%= image_tag @food.user.get_profile_image(100,100) %></p>
  <p>ユーザーネーム：<%= @food.user.name %></p>
  <p>投稿日：<%= @food.created_at.strftime('%Y/%m/%d') %></p>
</div>