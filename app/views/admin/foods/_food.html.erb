<% foods.each do |food| %>
  <div class="">
    <div class="card">
      <%= link_to admin_food_path(food.id) do %>
        <%= image_tag food.get_image[0], class:"card-img-top"; %>
      <% end %>
      <div class="card-body">
        <div class="row">
          <div class="col-2">
            <%= link_to admin_user_path(food.user) do %>
              <%= image_tag food.user.get_profile_image(100,100), class:"mt-3 rounded-circle"; %></p>
            <% end %>
          </div>
          <div class="col-10">
            <p class="card-title">
              <%= link_to(admin_food_path(food.id), class:"text-dark") do %>
                <strong><i class="far fa-flag"></i><span> <%= food.title %></span></strong>
              <% end %>
            </p>
            <p class="card-text"><i class="fas fa-map-marker-alt"></i> <%= food.address %></p>
            <p class="card-text"><i class="fas fa-book-open"></i> <%= food.body %></p>
            <span><i class="fas fa-user"></i> By <%= food.user.name %> ｜ </span>
            <span><%= link_to "#{food.comments.count} コメント", admin_food_path(food.id), class:"text-dark" %></span>
            <span>♥<%= food.favorites.count %> いいね</span>
            <P>
              <%= food.created_at.strftime('%Y/%m/%d %H:%M') %>
            </p>
            <p>
              <%= link_to "記事を削除", admin_food_path(food), method: :delete, class:"text-dark" %>
            </P>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
